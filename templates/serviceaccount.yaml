{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "openchoreo-single.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "openchoreo-single.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
